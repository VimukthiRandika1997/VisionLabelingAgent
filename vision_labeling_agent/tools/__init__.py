"""
Contains wrapper code or adapters for vision tools:
    - remote model wrappers (HTTP clients)
    - local model wrappers

This provide a unified interface so the agent can call
any tool abstractly.
"""